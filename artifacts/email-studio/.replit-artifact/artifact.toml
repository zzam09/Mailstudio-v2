kind = "web"
previewPath = "/"
title = "Email Composer Studio"
version = "1.0.0"
id = "artifacts/email-studio"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24073

[services.development]
run = "pnpm --filter @workspace/email-studio run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/email-studio", "run", "build" ]
publicDir = "artifacts/email-studio/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24073"
BASE_PATH = "/"
